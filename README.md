# brd-engine
A RAG pipeline that turns Business Requirements Documents into a queryable knowledge base — parsing, enrichment, chunking, vector embedding, and indexing — then lets you interact with that knowledge via your own LLM API key.
